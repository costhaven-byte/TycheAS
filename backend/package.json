{
  "name": "lucrator-backend",
  "private": true,
  "version": "1.0.0",
  "description": "Lucrator backend — Meta (Instagram + Facebook) integrations via a central MetaService. Built to scale into a multi-provider SaaS backend.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0",
    "morgan": "^1.10.0"
  }
}
